"use client";
import {useState} from "react";
import {signIn} from "next-auth/react";
import {useRouter} from "next/navigation";
import {Hotel,Eye,EyeOff,Loader2} from "lucide-react";
import {useLocale} from "@/components/LocaleProvider";
export default function Login(){const {locale,setLocale,t}=useLocale();const router=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [show,setShow]=useState(false);const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await signIn("credentials",{email,password,redirect:false});if(r?.error)setError("Invalid email or password.");else{router.push("/dashboard");router.refresh()}}catch{setError("An error occurred. Please try again.")}finally{setBusy(false)}}return <main className="min-h-screen bg-gradient-to-br from-slate-950 via-blue-950 to-slate-900 flex items-center justify-center p-6"><div className="w-full max-w-md"><div className="text-center text-white mb-8"><Hotel className="mx-auto mb-3 h-12 w-12 text-blue-300"/><h1 className="text-3xl font-bold">HotelPro</h1><p className="mt-2 text-blue-200">{t("Management System")}</p></div><section className="rounded-2xl bg-white p-8 shadow-xl"><div className="flex justify-between gap-3 items-start"><h2 className="text-2xl font-semibold text-slate-900">{t("Welcome back")}</h2><select aria-label={t("Language")} value={locale} onChange={e=>setLocale(e.target.value as "en"|"fr"|"rw")} className="rounded border p-1 text-sm"><option value="en">EN</option><option value="fr">FR</option><option value="rw">RW</option></select></div><p className="mt-2 mb-6 text-sm text-slate-500">{t("Sign in to your staff account")}</p><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-medium text-slate-700">{t("Email")}<input className="mt-1 w-full rounded-lg border border-slate-300 p-3" type="email" autoComplete="username" required placeholder={t("Enter your email")} value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-sm font-medium text-slate-700">{t("Password")}<span className="relative block"><input className="mt-1 w-full rounded-lg border border-slate-300 p-3 pr-12" type={show?"text":"password"} autoComplete="current-password" required placeholder={t("Enter your password")} value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" aria-label={t(show?"Hide password":"Show password")} onClick={()=>setShow(!show)} className="absolute right-3 top-4">{show?<EyeOff className="h-5 w-5"/>:<Eye className="h-5 w-5"/>}</button></span></label>{error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-red-700">{t(error)}</p>}<button className="flex w-full items-center justify-center gap-2 rounded-lg bg-blue-600 p-3 font-semibold text-white hover:bg-blue-700 disabled:opacity-50" disabled={busy}>{busy&&<Loader2 className="h-4 w-4 animate-spin"/>}{t(busy?"Signing in...":"Sign In")}</button></form><p className="mt-6 text-sm text-slate-500">{t("Use the email and password provided by your administrator.")}</p></section></div></main>}
