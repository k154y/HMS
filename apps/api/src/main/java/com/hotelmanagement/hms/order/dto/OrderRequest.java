package com.hotelmanagement.hms.order.dto; import jakarta.validation.constraints.*; import java.util.*; import java.math.*; public record OrderRequest(UUID customerId,UUID folioId,@NotBlank String destination,List<Item> items){public OrderRequest(UUID c,UUID f,String d){this(c,f,d,List.of());} public record Item(@NotNull UUID productId,@NotNull @DecimalMin("0.0001") BigDecimal quantity){}}
