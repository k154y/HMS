package com.hotelmanagement.hms.order.dto; import com.hotelmanagement.hms.order.model.*; import java.math.*; import java.util.*; public record OrderResponse(UUID id,UUID customerId,UUID folioId,String destination,OrderStatus status,BigDecimal total){public static OrderResponse from(Order e){return new OrderResponse(e.getId(),e.getCustomerId(),e.getFolioId(),e.getDestination(),e.getStatus(),e.getTotal());}}
